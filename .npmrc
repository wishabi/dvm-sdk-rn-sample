# @flipp packages are hosted in Flipp's JFrog Artifactory. Add your provisioned
# credentials to your user-level ~/.npmrc (see README); never commit them here.
@flipp:registry=https://flipplib.jfrog.io/artifactory/api/npm/dvm-sdk-npm-local/

# Everything outside the @flipp scope resolves from the public npm registry.
registry=https://registry.npmjs.org/
