import type { Annotation } from '@flipp/dvm-sdk-native';

/** The "clipped" badge overlaid on offers the user has clipped. */
export const CLIPPED_ANNOTATION: Annotation = {
  type: 'clipped',
  imageUrl:
    'https://f.wishabi.net/arbitrary_files/60368/1517002650/60368_circle.png',
  position: 'top-right',
};
